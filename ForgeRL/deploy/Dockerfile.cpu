FROM python:3.12-slim
WORKDIR /workspace/ForgeRL
COPY . .
RUN python -m pip install --upgrade pip \
    && pip install torch --index-url https://download.pytorch.org/whl/cpu \
    && pip install -e .
CMD ["python", "scripts/distributed_smoke.py"]
